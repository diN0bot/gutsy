h2= teamname
ul
  each contact in team
      h4 #{contact.name}
      ul
        for uri,type in contact
          if type == "github" || type == "mailto"
            li #{type}: <a href=#{uri}>#{uri}</a>
          else
            li #{type}: #{uri}
